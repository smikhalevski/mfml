{
  "name": "example",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "mfmlc": "mfmlc --rootDir ./src --outDir ./gen --include './messages/*.json' --adapter localeFilesAdapter"
  },
  "devDependencies": {
    "mfml-cli": "^1.0.0",
    "mfml-compiler": "^1.0.0"
  },
  "dependencies": {
    "mfml-runtime": "^1.0.0"
  }
}
