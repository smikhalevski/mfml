The example that shows how to compile translations into multiple modules and export them through an index file.

Use `npm ci && npm run mfmlc` to compile messages from `./src/messages` to `./src/gen`.
